import { DegreeTypePipe } from './degree-type.pipe';

describe('DegreeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
